import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#A2ABB2",
    secondary: "#040403",
    yellow: "#F3FB10",
    darkGray: "#787E7F",
    lightGray: "#B6B6B6",
  },
});

export default theme;
